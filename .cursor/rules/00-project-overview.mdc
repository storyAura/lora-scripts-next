---
description: 项目入口 — 是什么、关键路径、本地/公开命名、契约与内部文档位置
alwaysApply: true
---

# lora-scripts-story-next（SD Trainer Next）

基于秋叶 `Akegarasu/lora-scripts` 分叉的 **LoRA / 网络训练 WebUI**：`python gui.py` 拉起 FastAPI（`mikazuki`）、对接魔改版 `vendor/sd-scripts`、预编译前端 `frontend/dist/`、可选 `train_monitor/` 训练监控。整合包与云镜像另有固定入口脚本，见下表「契约」。

## 关键路径（速查）

| 路径 | 用途 |
|------|------|
| `gui.py` | 主程序（开发可加 `--dev`） |
| `mikazuki/` | 后端 API、schema、内嵌打标编辑器等 |
| `vendor/sd-scripts/` | 训练脚本库（含项目定制，非纯上游） |
| `frontend/dist/` | **预编译**前端；不从本仓库构建，需 patch 时改产物文件 |
| `train_monitor/` | 训练监控静态页 + `server.py` |
| `docs/` | **公开**文档，随仓库发布 |
| `scripts/` | **公开**运维脚本（portable / autodl / cli / patch 等） |
| `build-scripts/` | 整合包构建脚本 |
| `doc/local/` | **本地** Agent 交接（`AGENT_INTERNAL.md` 等）；见 **`local-docs-index.mdc`** |
| `.cursor/rules/` | Cursor 规则；本文件为总入口 |

## 命名契约：本地 vs 公开

| 目录 | 单复数 | Git | 用途 |
|------|--------|-----|------|
| `docs/` | 复数 `docs` | 跟踪 | 用户与贡献者可见的 Markdown |
| `doc/` | 单数 `doc` | **忽略** | 本地笔记、交接、Issue 调查；`doc/local/` 见 **`doc/local/README.md`**（新文件：`handover/`、`scratch/issue-{n}-*.md`、`assets/`） |
| `scripts/` | 复数 `scripts` | 跟踪 | 项目正式脚本（勿随意改名子目录：portable/autodl/cli 等有约定） |
| `script/` | 单数 `script` | **忽略** | 个人或 Agent 一次性脚本；建议 `script/ops/`、`script/scratch/` |

**禁止**：把应提交的文档写进 `doc/`；把应提交的脚本写进 `script/`；在 `docs/` 里堆仅本地使用的附件（应放 `doc/issues/` 等）。

## 契约路径（勿动文件名/路径）

完整列表与原因见 **`docs/repo-layout.md`**。包括但不限于：`gui.py`、`run_gui.bat`、`start_autodl.sh`、`setup_environment.py`、`requirements.txt`、`VERSION`；整合包根布局 `python_embeded/`、`SD-Trainer/` 及 `scripts/portable/launch_portable.bat`。

## Agent 内部交接（不上 GitHub）

**检索入口**：**`.cursor/rules/local-docs-index.mdc`**（`alwaysApply`，按任务列出必读路径）。

主文档：**`doc/local/AGENT_INTERNAL.md`**（GitHub Token / `gh` 与 REST 备选、整合包、端口、本机环境）。根目录若出现同名 `AGENT_INTERNAL.md` 仍被 `.gitignore` 忽略，请勿长期双份维护。

其它常见本地规划文档见索引规则（如 `doc/02_迁移技术难题与选择汇总.md`、`doc/双模式Turbo集成计划.md`、`doc/frontend-agent-guide.md`）。

## 团队协作（维护者）

分工、决策、Issue 流程、发布与风险备忘录：**`docs/team/README.md`**（所有待办必须进 GitHub Issue 才进入解决流程）。

## 其它 Cursor 规则

- **`local-docs-index.mdc`**：`doc/local/` 与 `doc/` 根目录重要 md 的必读索引（含 `AGENT_INTERNAL.md`）。
- **`embedded-service-ports.mdc`**：多服务端口、`/train-monitor`、禁止硬编码 `127.0.0.1:6008` 等。
